// Cloudflare（Workers 靜態資源）部署設定。沒有伺服器程式，只把整個網站當靜態檔案送到 Cloudflare 的節點。
//
// 🔴 這個檔一定要在 repo 裡：沒有它，`npx wrangler deploy` 會跑自動設定，把 wrangler 裝進專案的
// node_modules/，而上傳目錄是根目錄 ⇒ 連 126 MB 的 workerd 執行檔一起當成網頁檔案上傳，超過 25 MB 上限失敗
//（2026-09-17 在 kiyochika-pixel 實際發生過）。哪些檔案不上傳寫在 .assetsignore。
//
// 這個專案特別要注意兩件事，兩件都寫在 .assetsignore：
//   assets/          本機有 2.1 GB 原始掃描（.gitignore 擋掉了，但 wrangler 上傳的是本機目錄不是 git）
//   game-assets/bgm/ DOVA-SYNDROME 授權禁止再配布音源檔
{
  "name": "tokaido-pixel",
  "compatibility_date": "2026-09-16",
  "assets": {
    "directory": "."
  }
}
